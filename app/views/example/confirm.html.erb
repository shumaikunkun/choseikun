<!DOCTYPE html>
<html>
<head>
  <title>日程調整サイト</title>
  <%= stylesheet_link_tag 'application', media:'all', 'data-turbolinks-track'=>true %>
  <%= javascript_include_tag 'application', 'data-turbolinks-track'=>true %>
  <%= csrf_meta_tags %>
</head>

<body>
  <h1>日程調整サイト</h1>
  <div  class="big">
    <div class="small">
      <h2>投票完了しました。</h2>

      <p>名前：<%=@name %></p>
      <p>時間：<%=@time %></p>

      <table border="1">
        <tr>
          <td>日\時</td>
          <% @hour.each do |h| %>
          <td><%=h %></td>
          <% end %>
        </tr>
        <% @day.each do |d| %>
        <tr>
          <td><%=d %></td>
          <% @hour.each do |h| %>
          <td>
            <% if @test[@day.index(d)][@hour.index(h)]=="1" %>
            ⭕️
            <% else %>
            ✖️
            <% end %>
          </td>
          <% end %>
        </tr>
        <% end %>
      </table>
      <h2>投票結果は<%= link_to("こちら","http://localhost:3000/#{params[:num]}/show") %>をクリック</h2>
    </div>
  </div>
  <div class="under">
    <p class="copyright">&#169;2019 Shumaikun</p>
  </div>
</body>
</html>
